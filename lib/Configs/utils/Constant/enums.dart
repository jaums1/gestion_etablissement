
// ignore_for_file: constant_identifier_names

enum TextSizes {small,medium,large} 

enum OrderStatuts {processing,shipped,delivered,pending,cancelled} 

enum PaymentMetho {paypal,googlePay,appPay,visa,masterCard,creditCard,paystack,razorPay,paytm} 

enum AppRole {Admin,Secretaire,Enseignant}

enum TransactionType {buy,sell}

enum ProductType {single, variable}

enum ProductVisibility {published,hidden}

enum ImageType {asset,network,memory,file}

enum MedialCategory{folder,banners ,brands,categories,users,products}

enum TraitementAction {nouveau,modifier,supprimer,detail}

