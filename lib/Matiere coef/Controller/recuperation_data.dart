import 'package:get/get.dart';

import 'matierecoef_controller.dart';

class TRecuperationDataCoefficient {
  final controller = Get.find<TCoefficientController>();
  //// RECUPERATION NIVEAU
  void H_RecupeDataNiveau({String? param}){
    // controller.dataNiveauSerie = controller.controllerNiveauSerie.dataTableNiveauSerie.where((e)).
    // map(toElement).toList();
  }
  
}